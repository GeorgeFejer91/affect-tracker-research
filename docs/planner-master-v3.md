# Master3 controlled-renderer compatibility

Root schema freeze02 allocates this P7 pass and named P4/P6 dispatch seams.
Master3 requires workspace3/catalogue3 and mixed questionnaire contribution2.
The root schema remains affect-research-planner-recipe, version3; its integrity
algorithm is planner-recipe-reproduction-v4 and selected content is selection3.
Full controlled-renderer proof remains in P1 and selected assets, including
absent versus explicit source orientation. It is not independently observed
pixel orientation and does not grant playback qualification or media access.

Public master1 and master2 readers remain version-fenced, including P1v1/v2.
Supported dispatch admits3 explicitly. Opening or copying older files does not
upgrade or rewrite them. Fresh capture chooses3 only for accepted P1v3/P2v2;
P1v3 with P2v1 rejects. Source bytes and all nested provenance participate in
their existing canonical/hash boundaries; no catalogue3-to2 conversion exists.

The native master3 wrapper uses the same typed questionnaire/reproduction
implementation behind explicit version3 entrypoints. Master2 entrypoints still
require2. Layout receives only owner-validated dimensions through shared pure
calculation helpers; no P4/P6 units, math, profile or acceptance rules change.
P3's separately versioned internal catalogue context preserves its geometry-free
library and authored sequence hashes while retaining full P1 proof.

Synthetic engineering fixtures cover desktop and included XR profiles, all
language/variant selections, canonical master bytes and expected reproduction
and selected-content hashes. They are not researcher-authored native evidence.
54 focused JavaScript checks pass, including historical master1/2, strict
rejections, supported Save/Open/exact-copy and proof-only identity changes.
Rust cross-language tests are added but await assembled execution at this draft
checkpoint. Native producer/actor integration and actual CLI rebind/master
export remain separate gates. No foreground executable was replaced or opened.
